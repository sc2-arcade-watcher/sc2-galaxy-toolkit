{
  "name": "sc2-galaxy-toolkit",
  "private": true,
  "packageManager": "pnpm@9.15.4",
  "engines": { "node": ">=22.0.0" },
  "devDependencies": {
    "typescript": "catalog:",
    "@types/node": "catalog:",
    "vitest": "catalog:",
    "eslint": "catalog:",
    "typescript-eslint": "catalog:"
  },
  "scripts": {
    "build": "pnpm -r run build",
    "test": "pnpm -r run test",
    "lint": "pnpm -r run lint"
  }
}
